openapi: 3.0.3
info:
  title: New Legacy Operation Fixture API
  version: 1.1.0
paths:
  /api/widgets:
    post:
      operationId: createWidget
      x-stability-level: beta
      x-kubeorch-workspace-boundary: legacy-user
      x-kubeorch-required-scopes: [widgets:write]
      security:
        - bearerAuth: []
      responses:
        "201":
          description: Widget created
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
